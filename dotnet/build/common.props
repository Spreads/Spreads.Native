<Project>
  <!-- common.props is per repo config -->
  <Import Project="Spreads.props" />
  <PropertyGroup>
    <PackageTags>Spreads Native LZ4 Zstd Zlib GZip Unsafe shuffle SIMD pinvoke </PackageTags>
    <PackageProjectUrl>http://docs.dataspreads.io/spreads/libs/native/api/README.html</PackageProjectUrl>
    <RepositoryUrl>https://github.com/Spreads/Spreads.Native.git</RepositoryUrl>
  </PropertyGroup>
</Project>